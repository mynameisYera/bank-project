import 'package:flutter/material.dart';

class GreenTile extends StatelessWidget {
  const GreenTile({super.key});

  @override
  Widget build(BuildContext context) {
    return Container(
      child: Row(
        children: [],
      ),
    );
  }
}
